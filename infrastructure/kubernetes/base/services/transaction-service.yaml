apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: fraud-platform
  labels:
    app: transaction-service
    component: core
spec:
  replicas: 5
  selector:
    matchLabels:
      app: transaction-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: transaction-service
        component: core
    spec:
      containers:
        - name: transaction-service
          image: fraud-platform/transaction-service:latest
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: fraud-platform
  labels:
    app: transaction-service
    component: core
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
