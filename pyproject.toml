[project]
name = "fraud-detection-platform"
version = "0.1.0"
requires-python = ">=3.11"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.mypy]
strict = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
